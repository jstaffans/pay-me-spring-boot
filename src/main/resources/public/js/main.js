requirejs.config({
    paths: {
        app: '../js/app',
        /* Could use webjars locator, but then it would be hard to get the minified version */
        jquery: '/webjars/jquery/2.1.3/jquery.min'
    }
});

